name: CI/CD

on:
  push:
    branches:
      - main

jobs:
  Lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ./devops/scripts/lint.ps1
        shell: pwsh

  Build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ./devops/scripts/build.ps1
        shell: pwsh

  Provision:
    needs: [Build]
    if: success()
    runs-on: ubuntu-latest
    steps:
      - run: ./devops/scripts/provision.ps1

  Deploy:
    needs: [Provision]
    if: success()
    runs-on: ubuntu-latest
    steps:
      - uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      - run: echo "🎉"

  Cleanup:
    needs: [Deploy]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉"
